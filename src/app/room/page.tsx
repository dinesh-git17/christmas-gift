import type { JSX } from "react";

export default function RoomPage(): JSX.Element {
  return (
    <main className="flex min-h-svh flex-col items-center justify-center">
      {/* Room Decorator will go here */}
    </main>
  );
}
